/* Métodos Inmutables */
const names = ["Jonathan", "Aby", "Mata", "Migue"];
names.forEach((name) => console.log(name));
names.forEach((name) => console.log(name.toUpperCase));
console.log(names);

/* Métodos Mutables */
names.sort();
console.log(names);